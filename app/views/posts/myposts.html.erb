<%= stylesheet_link_tag("post")%>
<p style="color: green"><%= notice %></p>
<div id="posts" class="text-center">
  <% @posts.each do |post| %>
    <% if post.user_id == current_user.id.to_s %>
      <%= render post %>
    <% end %>
  <% end %>
</div>

